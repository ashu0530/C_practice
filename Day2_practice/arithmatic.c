#include<stdio.h>

int main(){
    int a = 4;
    int b = 8;

    //int z;

    //b*a = z; //we can't write in general mathematics way

    printf("the value of a+b is : %d\n", a+b);   // a and b is operand and + is operator
    printf("the value of a-b is : %d\n", a-b);
    printf("the value of a*b is : %d\n", a*b);
    printf("the value of a/b is : %d\n", a/b); 
    return 0;
}